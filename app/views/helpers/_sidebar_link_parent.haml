%li.nav-item{ class: ('active' if active_path?(path)) }
  %a.nav-link.nav-toggle{ href: 'javascript:;' }
    - if !fa_icon.nil?
      %i.fa{ class: "fa-#{fa_icon}" }
    %span.title= label.upcase
    %span.arrow{ class: ('open' if active_path?(path)) }
  %ul.sub-menu
    = children